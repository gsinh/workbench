/**
 * Re-exported from the shared palette.
 *
 * These colours were first validated for this experiment's stacked bars and
 * are now used by the others too, so they live in one place — keeping a second
 * copy here is how two charts quietly drift into two palettes.
 */
export * from "../shared/palette";
